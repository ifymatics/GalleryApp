import { shallow } from "enzyme";
import ImageDetails from "./ImageDetails";

describe("ImageDetails Component", () => {
  it("renders without crashing", () => {
    // const wrapper = shallow(<ImageDetails />);
    // expect(wrapper.exists()).toBe(true);
  });
});
